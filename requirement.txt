# Core Django packages
django
djangorestframework

# PostgreSQL support
psycopg2-binary  # PostgreSQL database driver

# Task queue & messaging
celery
redis

# Filtering & CORS
django-cors-headers
django-filter

# Environment variable management
django-environ

# Optional: JWT authentication
djangorestframework-simplejwt

# Optional: For production deployment (Gunicorn + static files)
#gunicorn
#whitenoise
