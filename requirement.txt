django
djangorestframework
celery
redis
django-cors-headers
django-filter
#django-mssql-backend  # SQL Server backend for Django
#pyodbc  # Required for SQL Server connection
django-cors-headers  # If handling CORS
django-environ  # For environment variable management
#djangorestframework-simplejwt  # For JWT authentication
#gunicorn  # If deploying with a WSGI server
#whitenoise  # For serving static files

#Ubuntu/Linux:
#sudo apt update
#sudo apt install unixodbc-dev
#sudo apt install msodbcsql17